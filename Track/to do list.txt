OK 	1. 在TRT线程里完成手动控制，用ch作switch
	2. 记录光栅尺读数用单独一个线程，不用与其他线程同步，通过TCP与低倍图像记录电脑连接，由其触发
	3. 鱼被水流冲走时，对鱼的运动预测会不会失效？水流冲击带动鱼的速度如果远小于鱼游动的瞬时速度，就没有太大影响。
OK	4. 队列维护，考虑长度变化时的操作！！！！！
OK	5. 参数换成params里的
OK	6. vector超过capacity后会重新分配内存，iterator会失效，这是一个隐患。如果从定义iterator到使用它，中间没有向vector中增加元素，就没有问题。
OK	7. call back里对flag_cb，action的操作
	8. 通过TCP与纪静远的程序通信，传输光栅尺读数，不用与其他线程同步，
OK	9. GUI需要单独一个线程，否则运行到run()就会停住。
OK	10. 电压输入接口初始化时，要选择dev1或dev2，取决于卡插在哪个卡槽。
OK	11. 切换手动或追踪模式时，相机状态不变，从程序开始运行，相机就处于拍摄状态，知道程序结束。否则会出问题。